<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jockey - Join Now</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background: #f8f9fa;
            padding: 0;
            position: relative;
            overflow-x: auto;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem 1rem;
            min-height: 100vh;
        }

        /* Header */
        .page-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .page-header h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin: 0 0 0.5rem 0;
            color: #2c3e50;
        }

        .page-header p {
            font-size: 1.1rem;
            color: #7f8c8d;
            margin: 0;
        }

        /* Main Container */
        .registration-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 3rem;
            max-width: 1000px;
            margin: 0 auto;
        }

        .registration-form,
        .benefits-sidebar {
            background: #fff;
            border: 1px solid #e0e0e0;
            padding: 2.5rem;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            border-radius: 5px;
        }

        /* Pointy buttons */
        .btn {
            display: inline-block;
            padding: 12px 24px;
            border: 2px solid transparent;
            font-weight: 600;
            cursor: pointer;
            text-align: center;
            font-size: 1rem;
            text-decoration: none;
            transition: all 0.3s ease;
            box-sizing: border-box;
            border-radius: 0 !important; /* Pointy corners */
        }

        .btn-fullwidth {
            width: 100%;
        }

        .btn-primary {
            background: #3498db;
            color: white;
            border-color: #3498db;
        }

        .btn-primary:hover {
            background: #2980b9;
            border-color: #2980b9;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

        .btn-black {
            background: #2c3e50;
            color: white;
            border-color: #2c3e50;
        }

        .btn-black:hover {
            background: #1a252f;
            border-color: #1a252f;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

        .btn-outline {
            background: transparent;
            border-color: #3498db;
            color: #3498db;
        }

        .btn-outline:hover {
            background: #3498db;
            color: white;
        }

        /* Form Elements */
        .input-group {
            margin-bottom: 1.5rem;
        }

        .input-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #2c3e50;
        }

        .input-field {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ddd;
            font-size: 1rem;
            background: #fafafa;
            transition: all 0.3s ease;
            border-radius: 0; /* Pointy corners */
        }

        .input-field:focus {
            outline: none;
            border-color: #3498db;
            background: #fff;
            box-shadow: 0 0 0 3px rgba(52,152,219,0.1);
        }

        /* Benefits list */
        .benefits-list {
            list-style: none;
            padding: 0;
            margin: 0 0 2rem 0;
        }

        .benefits-list li {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 0;
            font-weight: 500;
            font-size: 1.1rem;
            border-bottom: 1px solid #f0f0f0;
        }

        .benefits-list li:last-child {
            border-bottom: none;
        }

        .benefits-list .icon {
            font-size: 1.5rem;
            min-width: 40px;
            color: #3498db;
        }

        .hide {
            display: none !important;
        }

        .form-success {
            background: #d4edda;
            color: #155724;
            padding: 0.875rem;
            margin-bottom: 1.5rem;
            border: 1px solid #c3e6cb;
            border-radius: 0;
        }

        .errors {
            background: #f8d7da;
            color: #721c24;
            padding: 0.875rem;
            margin-bottom: 1.5rem;
            border: 1px solid #f5c6cb;
            border-radius: 0;
        }

        .forgot-password {
            text-align: right;
            margin-bottom: 1.5rem;
        }

        .forgot-password a {
            color: #3498db;
            text-decoration: none;
            font-size: 0.9rem;
        }

        .forgot-password a:hover {
            text-decoration: underline;
        }

        .back-link {
            text-align: center;
            margin-top: 1rem;
        }

        .back-link a {
            color: #3498db;
            text-decoration: none;
            font-size: 0.9rem;
        }

        .back-link a:hover {
            text-decoration: underline;
        }

        .form-submit {
            margin-bottom: 1rem;
        }

        .benefits-header {
            margin-bottom: 2rem;
        }

        .benefits-header h2 {
            font-size: 1.8rem;
            font-weight: 700;
            margin: 0 0 1rem 0;
            color: #2c3e50;
            text-align: center;
        }

        .benefits-header p {
            color: #7f8c8d;
            text-align: center;
            margin-bottom: 2rem;
        }

        .join-note {
            color: #7f8c8d;
            font-size: 0.9rem;
            font-style: italic;
            margin: 10px 0 0 0;
            text-align: center;
        }

        .auth-header h2 {
            font-size: 1.8rem;
            font-weight: 600;
            margin: 0 0 1.5rem 0;
            color: #2c3e50;
            text-align: center;
        }

        .auth-header p {
            color: #7f8c8d;
            margin: 0 0 2rem 0;
            text-align: center;
        }

        .return-link {
            text-align: center;
            margin-top: 2rem;
            padding-top: 1rem;
            border-top: 1px solid #e0e0e0;
        }

        .return-link a {
            color: #3498db;
            text-decoration: none;
            font-weight: 500;
        }

        .return-link a:hover {
            text-decoration: underline;
        }
        
        .password-help {
            display: block;
            margin-top: 0.5rem;
            color: #7f8c8d;
            font-size: 0.85rem;
        }
        
        .checkbox-group {
            margin: 1.5rem 0;
        }
        
        .checkbox-label {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            cursor: pointer;
        }
        
        .divider-text {
            text-align: center;
            position: relative;
            margin: 2rem 0;
            color: #7f8c8d;
        }
        
        .divider-text::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 1px;
            background: #e0e0e0;
            z-index: 1;
        }
        
        .divider-text span {
            background: white;
            padding: 0 1rem;
            position: relative;
            z-index: 2;
        }

        .already-member {
            text-align: center;
            margin-top: 2rem;
            padding: 1.5rem;
            background: #f8f9ff;
            border: 1px solid #e6e9ff;
            border-radius: 0;
        }

        .already-member p {
            margin-bottom: 1rem;
            color: #2c3e50;
        }

        /* Responsive */
        @media (max-width: 900px) {
            .registration-container {
                grid-template-columns: 1fr;
                gap: 2rem;
                max-width: 600px;
            }
            
            .page-header h1 {
                font-size: 2.2rem;
            }
            
            .registration-form,
            .benefits-sidebar {
                padding: 2rem;
            }
        }

        @media (max-width: 600px) {
            .container {
                padding: 1.5rem 1rem;
            }
            
            .page-header {
                margin-bottom: 2rem;
            }
            
            .page-header h1 {
                font-size: 2rem;
            }
            
            .registration-form,
            .benefits-sidebar {
                padding: 1.5rem;
            }
            
            .benefits-list li {
                font-size: 1rem;
            }
            
            .benefits-list .icon {
                font-size: 1.3rem;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 1rem 0.5rem;
            }
            
            .page-header h1 {
                font-size: 1.75rem;
            }
            
            .registration-form,
            .benefits-sidebar {
                padding: 1.25rem;
            }
            
            .auth-header h2 {
                font-size: 1.5rem;
            }
            
            .benefits-header h2 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Page Header -->
        <div class="page-header">
            <h1>Join Jockey</h1>
            <p>Create your account to enjoy exclusive member benefits</p>
        </div>

        <!-- Main Registration Section -->
        <div class="registration-container">
            <!-- Registration Form -->
            <div class="registration-form">
                <div class="auth-header">
                    <h2>Create Your Account</h2>
                    <p>Fill in your details to get started</p>
                </div>

                <form>
                    <div class="input-group">
                        <label for="RegisterFirstName">First Name</label>
                        <input type="text"
                              name="customer[first_name]"
                              id="RegisterFirstName"
                              class="input-field"
                              placeholder="First name">
                    </div>

                    <div class="input-group">
                        <label for="RegisterLastName">Last Name</label>
                        <input type="text"
                              name="customer[last_name]"
                              id="RegisterLastName"
                              class="input-field"
                              placeholder="Last name">
                    </div>

                    <div class="input-group">
                        <label for="RegisterEmail">Email Address</label>
                        <input type="email"
                              name="customer[email]"
                              id="RegisterEmail"
                              class="input-field"
                              placeholder="Enter your email address"
                              spellcheck="false"
                              autocomplete="email"
                              autocapitalize="off">
                    </div>

                    <div class="input-group">
                        <label for="RegisterPassword">Password</label>
                        <input type="password"
                              name="customer[password]"
                              id="RegisterPassword"
                              class="input-field"
                              placeholder="Create a password">
                        <small class="password-help">Password must be at least 5 characters long</small>
                    </div>

                    <div class="checkbox-group">
                        <label class="checkbox-label">
                            <input type="checkbox" name="customer[accepts_marketing]">
                            I'd like to receive exclusive offers and updates via email
                        </label>
                    </div>

                    <div class="form-submit">
                        <input type="submit" class="btn btn-black btn-fullwidth" value="Create Account">
                    </div>
                </form>

                <div class="already-member">
                    <p>Already have an account?</p>
                    <a href="#" class="btn btn-outline btn-fullwidth">Sign In</a>
                </div>
            </div>

            <!-- Benefits Sidebar -->
            <div class="benefits-sidebar">
                <div class="benefits-header">
                    <h2>Member Benefits</h2>
                    <p>Join Jockey and enjoy these exclusive perks</p>
                </div>

                <ul class="benefits-list">
                    <li><span class="icon"><i class="fas fa-percent"></i></span> <strong>10% off all purchases</strong> - Enjoy discounts on every order</li>
                    <li><span class="icon"><i class="fas fa-shipping-fast"></i></span> <strong>Free shipping</strong> - On all orders over $50</li>
                    <li><span class="icon"><i class="fas fa-box"></i></span> <strong>Easy order tracking</strong> - Monitor your purchases in real-time</li>
                    <li><span class="icon"><i class="fas fa-gift"></i></span> <strong>Exclusive rewards</strong> - Special birthday offers and gifts</li>
                    <li><span class="icon"><i class="fas fa-calendar"></i></span> <strong>Early access to sales</strong> - Shop before everyone else</li>
                    <li><span class="icon"><i class="fas fa-undo"></i></span> <strong>Easy returns</strong> - Hassle-free return policy</li>
                </ul>

                <p class="join-note">Plus exclusive access to member-only sales and promotions!</p>
            </div>
        </div>

        <!-- Return Link -->
        <div class="return-link">
            <a href="#"><i class="fas fa-arrow-left"></i> Return to Store</a>
        </div>
    </div>

    <script>
        // Simple form validation for demo
        (function() {
            var form = document.querySelector('.registration-form form');
            
            if (form) {
                form.addEventListener('submit', function(e) {
                    e.preventDefault();
                    
                    // Simple validation
                    var firstName = document.getElementById('RegisterFirstName').value;
                    var lastName = document.getElementById('RegisterLastName').value;
                    var email = document.getElementById('RegisterEmail').value;
                    var password = document.getElementById('RegisterPassword').value;
                    
                    if (!firstName || !lastName || !email || !password) {
                        alert('Please fill in all required fields.');
                        return;
                    }
                    
                    if (password.length < 5) {
                        alert('Password must be at least 5 characters long.');
                        return;
                    }
                    
                    alert('Account created successfully! (This is a demo)');
                });
            }
        })();
    </script>
</body>
</html>